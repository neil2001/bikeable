/**
 * OpenFreeMap Positron — quiet light OSM vector basemap.
 * Free public instance: no API key, no watermarks. Keep attribution visible.
 * Same OSM data as the backend OSMnx graph, so overlays align with streets.
 *
 * https://openfreemap.org/
 */
export const OPENFREEMAP_POSITRON_STYLE = "https://tiles.openfreemap.org/styles/positron";
